// GLOBALS

// __dirname
// __filemane
// require => to import a module in another, by specifying its path
// module => every file is a module, we can export values with module.exports = something to make it importable anywhere
//process